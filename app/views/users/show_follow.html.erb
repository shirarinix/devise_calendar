<p>フォロー一覧</p>

<table>
<% if current_user.present? %>
  <% @user.followings.each do |user| %>
    <tr>
      <td> <%= user.nickname %> </td>
      <td><%= link_to '情報一覧へ', user_path(user) %></td>
    </tr>
  <% end %>
<% else %>
  <% @artist.artist_follow_ids.each do |artist| %>
    <tr>
      <td> <%= artist.artistname %> </td>
      <td><%= link_to '情報一覧へ', artist_path(artist) %></td>
    </tr>
  <% end %>
<% end %>
</table>