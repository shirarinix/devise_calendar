[ログイン者]
<% if current_user.present? %>
  <%= current_user.nickname %>
<% else %>
  <%= current_artist.artistname %>
<% end %><br>
<br>

<p id="flash"><%= flash[:notice] %></p>

<ul class="user-form">
  <li>
    <% if @user.user_image.attached? %>
      <%= image_tag @user.user_image, size:'50', class:"user-icon" %>
    <% end %>
    <%= @user.nickname %>さんのページ！！
  </li>
  <li>
    <%= render 'bookmarks/user_follow_form', artist: @artist %>
  </li>
</ul>

<ul class= "user-profile">
</ul>

<%= link_to "← トップへ戻る", root_path %>
  ⑴.<%= @user.followings.count %>
  ⑵.<%= @user.artist_followings.select("follow_id").count %>
  ⑶.<%= @user.followers.count %>
  ⑷.<%= @artist.user_followed_ids.select("followed_id").count %>

<%= render 'events/user_events_calender' %>