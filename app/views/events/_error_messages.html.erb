<%# イベントのエラーメッセージ %>
<ul class="event-errors">
<% if @event.errors.any? %>
  <div class="alert alert-warning">
    <ul>
      <% @event.errors.full_messages.each do |message| %>
        <li class="event-error-message"><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
</ul>