<% if current_user %>
  <% if current_user.id == @event.user_id %>
    <%= link_to '★このイベントを編集する★', edit_event_path(@event) %><br>
    <%= link_to '★新しくイベントを作成する★', new_event_path(@event) %><br>
    <%= link_to '★このイベントを削除する★', event_path(@event), method: :delete, data: { confirm: '本当に削除してよろしいですか？？' } %><br>
    <%= link_to '← uトップへ戻る', root_path %><br>
    <%= render 'simple_calendar/events_calendar' %>
  <% else %>
    <strong><p>※※※ このイベントの編集は本人しか変更出来ません！！ ※※※</p></strong>
    <%= link_to '← uトップへ戻る', root_path %>
    <%= render 'simple_calendar/events_calendar' %>
  <% end %>
<% else %>
  <% if current_artist.id == @event.artist_id %>
    <%= link_to '★このイベントを編集する★', edit_event_path(@event) %><br>
    <%= link_to '★新しくイベントを作成する★', new_event_path(@event) %><br>
    <%= link_to '★このイベントを削除する★', event_path(@event), method: :delete, data: { confirm: '本当に削除してよろしいですか？？' } %><br>
    <%= link_to '← aトップへ戻る', root_path %><br>
    <%= render 'simple_calendar/events_calendar' %>
  <% else %>
    <strong><p>※※※ このイベントの編集は本人しか変更出来ません！！ ※※※</p></strong>
    <%= link_to '← aトップへ戻る', root_path %>
    <%= render 'simple_calendar/events_calendar' %>
  <% end %>
<% end %>