<h3>◉devise_carender◉</h3>

<p id="notice"><%= notice %></p>

<ul class="user-ver">
<% if signed_in? %>
[ログイン者]
  <% if current_user %>
    <li><%= link_to current_user.nickname, "#", class: "user-nickname" %></li>
    <li><%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: "logout" %></li>
  <% else %>
    <li><%= link_to current_artist.artistname, "#", class: "artistname" %></li>
    <li><%= link_to "ログアウト", destroy_artist_session_path, method: :delete, class: "logout" %></li>
  <% end %>
<% else %>
<p>●ユーザー登録●</p>
  <li><%= link_to "User-sign-up", new_user_registration_path, class: "signup" %></li>
  <li><%= link_to 'user-log-in', new_user_session_path, class: "login" %></li>
<p>●アーティスト登録●</p>
  <li><%= link_to "Artist-sign-up", new_artist_registration_path, class: "signup" %></li>
  <li><%= link_to "artist-log-in", new_artist_session_path, class: "login" %></li>
<% end %>
</ul>

<% if signed_in? %>
<p>[ユーザー一覧]</p>
<ul class="user-list">
  <% @users.each do |user| %>
    <li>
      <% if user.user_image.attached? %>
        <%= image_tag user.user_image, size:"50", class:"user-icon" %>
      <% else %>
        <i class="far fa-grin-alt fa-3x"></i>
      <% end %>
      <%= link_to "※#{user.nickname}さんのページ※", user_path(user) %>
    </li>
  <% end %>
</ul>
<p>[アーティスト一覧]</p>
<ul class="artist-list">
  <% @artists.each do |artist| %>
  <li>
    <% if artist.artist_image.attached? %>
      <%= image_tag artist.artist_image, size:'50', class:"artist-icon" %>
    <% else %>
      <i class="far fa-grin-alt fa-3x"></i>
    <% end %>
    <%= link_to "※#{artist.artistname}さんのページ※", artist_path(artist) %>
  </li>
  <% end %>
</ul>
<% end %>

<% if signed_in? %>
  <h3>🔻スケジュールの管理🔻</h3>
  <%= link_to "→ 新規イベントの作成", new_event_path %><br>
<table>
  <p>⭐️直近のイベント⭐️</p>
  <thead>
    <tr>
      <th>『ユーザー名』</th>
      <th>『イベントタイトル』</th>
      <th>『開催日（時間）』</th>
      <th colspan="3">『詳細一覧』</th>
    </tr>
  </thead>
    <% if current_user %>
      <% @user_events.each do |event| %>
        <tbody>
          <tr>
            <td><%= event.user.nickname %>さん</td>
            <td><%= "🚩 #{event.title}" %></td>
            <td><%= event.start_time.strftime("%Y-%m-%d %H:%M ~ ") %></td>
            <td><%= link_to "→詳細ページへ", event_path(event) %></td>
          </tr>
        </tbody>
      <% end %>
    <% else %>
      <% @artist_events.each do |event| %>
        <tbody>
          <tr>
            <td><%= event.artist.artistname %>さん</td>
            <td><%= "🚩 #{event.title}" %></td>
            <td><%= event.start_time.strftime("%Y-%m-%d %H:%M ~ ") %></td>
            <td><%= link_to "→詳細ページへ", event_path(event) %></td>
          </tr>
        </tbody>
      <% end %>
    <% end %>
<% else %>
  <strong><p>※※※ イベントの入力には新規登録/ログインが必要です ※※※</p></strong>
<% end %>
</table>

<br>⭐️今月の主なイベント⭐️<br>
<% if signed_in? %>
  <% if current_user %>
    <%= render 'events/user_events_calender' %>
  <% else %>
    <%= render 'events/artist_events_calender' %>
  <% end %>
<% else %>
  <strong><p>※※※ カレンダーの表示には新規登録/ログインが必要です ※※※</p></strong>
<% end %>
