<h1>詳細画面</h1>

<p id="notice"><%= notice %></p>

💫<%= @event.user.nickname %>さんの情報💫

<p>
  <strong>【タイトル】</strong>
  <%= @event.title %>
</p>

<p>
  <strong>【更新日時】</strong>
  <%= @event.start_time.strftime ("%Y-%m-%d %H:%M") %>
</p>

<p>
  <strong>【イベント内容】</strong>
  <%= @event.content %>
</p>

<% if user_signed_in? && current_user.id == @event.user.id %>
  <%= link_to '★現在のイベントを編集する★', edit_event_path(@event) %><br>
  <%= link_to '★新しくイベントを作成する★', new_event_path(@event) %><br>
  <%= link_to '★このイベントを削除する★', event_path(@event), method: :delete, data: { confirm: '本当に削除してよろしいですか？？' } %><br>
  <%= link_to '⬅️ トップへ戻る', root_path %>
  <br>
  <%= render 'events/event-calender' %>
<% else %>
  <strong><p>※※※ このイベントの変更は本人しか参照出来ません！！ ※※※</p></strong>
  <%= link_to '⬅️ トップへ戻る', root_path %>
<% end %>