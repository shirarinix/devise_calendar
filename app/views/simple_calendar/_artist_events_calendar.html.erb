<%#= アーティストのカレンダーを表示 %>
<div>⭐️今月のカレンダー⭐️</div>
<%= month_calendar events: @artist_events do |date, event| %>
<%= date.day %>
  <% event.each do |event| %>
    <div>
      <%= link_to "🚩 #{event.title}", event, class:"event-artist-title" %>
    </div>
  <% end %>
<% end %>